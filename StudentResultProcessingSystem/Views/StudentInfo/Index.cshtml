@* @model StudentViewModel

<h2>Student Form</h2>

<form asp-action="Index" method="post">
    <input type="hidden" asp-for="Id" />

    <div>
        <label asp-for="StudentName"></label>
        <input asp-for="StudentName" />
        <span asp-validation-for="StudentName" class="text-danger"></span>
    </div>
    <div>
        <label>Roll</label>
        <input asp-for="Roll" />
    </div>
    <div>
        <label>Email</label>
        <input asp-for="Email" />
    </div>
    <div>
        <label>Department</label>
        <select asp-for="departmentId"
                asp-items="@(new SelectList(Model.departments, "DepartmentId", "DepartmentName"))">
            <option value="">-- Select Department --</option>
        </select>
    </div>

    <button type="submit">Save</button>
</form>

<hr />

<h2>Student List</h2>

<table border="1">
    <tr>
        <th>Name</th>
        <th>Department</th>
        <th>Roll</th>
        <th>Email</th>
        <th>Actions</th>
    </tr>
   
         @foreach (var item in Model?.students)
        {
            <tr>
                <td>@item.StudentName</td>
                <td>@item.Department?.DepartmentName</td>
                <td>@item.Roll</td>
                <td>@item.Email</td>

                <td>
                    <!-- VIEW (just show data in form area) -->
                    <a asp-action="Index" asp-route-id="@item.StudentId">View/Edit</a>

                    <!-- DELETE -->
                    <form asp-action="Delete" method="post" style="display:inline;">
                        <input type="hidden" name="id" value="@item.StudentId" />
                        <button type="submit" onclick="return confirm('Delete this student?')">
                            Delete
                        </button>
                    </form>
                </td>
            </tr>
  }

    
    
    
</table> *@

                    @model StudentViewModel

@{
    ViewData["Title"] = "Student Management";
}

<div class="container mt-4">

    <!-- Form Card -->

    <div class="card shadow-lg border-0 mb-4">

        <div class="card-header bg-primary text-white">

            <h3 class="mb-0">
                Student Registration
            </h3>

        </div>

        <div class="card-body">

            <form asp-action="Index" method="post">

                <input type="hidden" asp-for="Id" />

                <div class="row">

                    <div class="col-md-6 mb-3">

                        <label asp-for="StudentName" class="form-label fw-bold"></label>

                        <input asp-for="StudentName" class="form-control" placeholder="Enter Student Name" />

                        <span asp-validation-for="StudentName" class="text-danger"></span>

                    </div>

                    <div class="col-md-6 mb-3">

                        <label class="form-label fw-bold">Roll</label>

                        <input asp-for="Roll" class="form-control" placeholder="Enter Roll" />

                    </div>

                    <div class="col-md-6 mb-3">

                        <label class="form-label fw-bold">Email</label>

                        <input asp-for="Email" class="form-control" placeholder="Enter Email" />

                    </div>

                    <div class="col-md-6 mb-3">

                        <label class="form-label fw-bold">Department</label>

                        <select asp-for="departmentId"
                                class="form-select"
                                asp-items="@(new SelectList(Model.departments,"DepartmentId","DepartmentName"))">

                            <option value="">-- Select Department --</option>

                        </select>

                    </div>

                </div>

                <div class="mt-3">

                    <button class="btn btn-success">

                        💾 Save

                    </button>

                    <a asp-action="Index" asp-route-id="0" class="btn btn-secondary">

                        🔄 Reset

                    </a>

                </div>

            </form>

        </div>

    </div>


    <!-- Student List -->

    <div class="card shadow-lg border-0">

        <div class="card-header bg-dark text-white">

            <h3 class="mb-0">

                Student List

            </h3>

        </div>

        <div class="card-body">

            <div class="table-responsive">

                <table id="tblStudent" class="table table-hover table-bordered align-middle">

                    <thead class="table-primary">

                        <tr>

                            <th>ID</th>

                            <th>Name</th>

                            <th>Department</th>

                            <th>Roll</th>

                            <th>Email</th>

                            <th width="170">Action</th>

                        </tr>

                    </thead>

                    <tbody>

                        @foreach (var item in Model.students)
                        {
                            <tr>

                                <td>@item.StudentId</td>

                                <td>@item.StudentName</td>

                                <td>@item.Department?.DepartmentName</td>

                                <td>@item.Roll</td>

                                <td>@item.Email</td>

                                <td>

                                    <a asp-action="Index"
                                       asp-route-id="@item.StudentId"
                                       class="btn btn-warning btn-sm">

                                        ✏ Edit

                                    </a>

                                    <form asp-action="Delete"
                                          method="post"
                                          class="d-inline">

                                        <input type="hidden"
                                               name="id"
                                               value="@item.StudentId" />

                                        <button class="btn btn-danger btn-sm"
                                                onclick="return confirm('Delete this student?')">

                                            🗑 Delete

                                        </button>

                                    </form>

                                </td>

                            </tr>
                        }

                    </tbody>

                </table>

            </div>

        </div>

    </div>

</div>


@section Scripts {

    <script>

        $(document).ready(function () {

            $('#tblStudent').DataTable({

                responsive: true,

                pageLength: 5,

                lengthMenu: [[5,10,25,50,-1],[5,10,25,50,"All"]],

                language: {

                    search: "🔍 Search Student:",

                    lengthMenu: "Show _MENU_ Students",

                    info: "Showing _START_ to _END_ of _TOTAL_ Students",

                    paginate: {

                        previous: "Previous",

                        next: "Next"

                    }

                }

            });

        });

    </script>

}